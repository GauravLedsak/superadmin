# CHANGELOG

Structured documentation started: 2026-07-21

---

<!-- Format: YYYY-MM-DD · <what changed> · <why> -->
2026-07-21 · Created full feature docs for Dashboard, Clients/Tenant360, and Security & Access with every tab, column, button, action, and hardcoded value documented from source code.
2026-07-21 · Completed feature docs for all 19 nav routes; documented dead code (BillingPage, RevenuePage defined but not routed); documented Pricing Engine logic and all seeded data.
2026-07-21 · LeadsPage fully rebuilt in superadmin/src: replaced static read-only overview with full lead management console — localStorage-backed lead state, PII access gating with 4h time-boxed grants, failure incident grouping, duplicate review modal, reprocess workflow, per-row actions, bulk select/export, audit trail, and clickable KPI tiles; source BarList replaced with table including Conv% and Cost/Lead columns.
2026-07-21 · LeadsPage: added Integration Health table (source-level health rollup replacing per-incident banners); auth failure classification (401 failures excluded from reprocess, routed to "Fix in Integrations →" in row action, drawer, and header button); Lead Processing Overview card renamed from Processing Status and gains trend deltas; page description updated to reflect internal-tool framing.
2026-07-22 · AutomationPage fully rebuilt with 3 tabs: Lead Routing (existing workflow + rules, now with functional New Workflow modal), Internal Ops (new full-CRUD tenant-state automation engine with localStorage persistence, run-now evaluation, and logs), Automation Logs (paginated run history with filters, drawer, CSV export). Integrations page updated: IndiaMART health now live-computed from leads; deep-link from Leads ("Fix in Integrations →") bumps and highlights the matching integration card.
2026-07-23 · Fixed page-level scroll bug on Clients (and other FIXED_HEIGHT_PAGES): Layout's outer frame and `<main>` used `min-h-screen` instead of a hard `h-screen`/`min-h-0`, so the whole document scrolled instead of just the table body — because `min-height` only sets a floor, not a cap.
2026-07-23 · LogsPage fully rebuilt as a 7-tab "Logs & Audit" console (System, API, Webhook, Integration, Security, Error, Admin Audit), replacing the static 6-row hardcoded table — new self-contained data/logs.js module (seed data, masking, retention policy, RBAC matrix, filtering/export helpers, localStorage-backed mutable state) plus shared filter bar, date-range/severity/multi-select filters, sortable columns, per-type detail drawers (retry timeline, before/after diff, stack trace, integration health cards, API stats header), cross-log correlation ("Related Entries"), CSV/JSON export, and append-only enforcement for Security and Admin Audit (no edit/delete code path exists for either).
2026-07-23 · Fixed table row-border "stepping" visible on colored/highlighted rows (Logs Security tab, Admin Audit, and every other table app-wide): Table/Td used per-cell `border-b` under `border-collapse: collapse`, which Chromium resolves per-cell at fractional zoom/DPR, landing adjacent columns' border lines on different sub-pixel rows. Replaced with an inset `box-shadow` on `<th>`/`Td`, which paints independently per cell with no collapse resolution. Same fix applied to LeadsPage's independent Lead Sources table (its own raw `<table>`, not using shared Table/Td) and the Admin Audit before/after diff table.
2026-07-23 · Fixed the same row-border misalignment surviving on 3 specific columns (Logs Security "Event Type", Error "Severity", Admin Audit "Action") after the box-shadow fix — root cause was `display:flex` applied directly to those `<Td>` cells (to lay out an icon/badge next to text), which pulls a cell out of normal table-cell box participation so its row height/bottom edge doesn't reconcile with sibling cells. Moved the flex layout to an inner `<div>` inside each Td instead.
2026-07-23 · SecurityPage fully rebuilt as a 7-tab "Security & Access Control" console (Admin Users, Roles & Permissions, Sessions, IP Restrictions, Login History, Security Alerts, API Keys), replacing the 3-tab hardcoded stub (Roles/Sessions/Policies, all toast-only). New data/security.js module (permission-matrix schema across 8 sections, seed data for all 7 entities, CIDR/key-generation helpers); StoreContext.jsx extended with adminUsers/secRoles/sessions/ipRestrictions/loginHistory/securityAlerts/apiKeys/sessionPolicy state and ~24 mutation methods, all following the addHistory+notify convention. Centerpiece is a sectioned permission-matrix editor (scope dropdown for "view", checkboxes for other actions, "—" for unsupported actions) matching the LEDSAK app's existing permission UI pattern. Manages the internal LEDSAK admin-team pool only — distinct from UsersPage's tenant end-users.
2026-07-23 · Removed MFA from Security & Access — LEDSAK doesn't have that feature. Dropped admin MFA enrollment fields/badge/filter/toggle, the mfa.challenge/mfa.verified login events, the mfa_disabled alert type, and the MFA-specific policy toggles (require-for-all, require-for-privileged, backup codes, allowed methods). Renamed the remaining general auth settings (max failed attempts, lockout duration, session timeout) from "MFA Config" to "Session Policy" (`store.updateSessionPolicy`). Replaced the "MFA Enrolled %" KPI with "Privileged Admins".
2026-07-23 · Added Webhook Keys as an 8th Security & Access tab — per-tenant outbound webhook subscriptions, distinct from API Keys (inbound REST access). Creation is a 3-step wizard: select client (reuses store.clients) → select modules (Leads/Users/Subscriptions/Payments/Onboarding/Automation) → multi-select events scoped to the chosen modules, plus name and destination URL. One-time signing-secret reveal on create/regenerate; Revoke sets status Revoked. New data/security.js WEBHOOK_MODULES taxonomy deliberately matches the event names already seeded in Logs & Audit's Webhook Logs tab.
2026-07-23 · Added a Global Automation KPIs panel to Automation Center, above the tab bar on every tab — 6 tiles (Automations Configured, Total Runs, Success Rate, Failure Rate, Needs Attention, MTD) with a 6-month dense sparkline each, combining real Internal Ops run logs with a newly-mocked Tenant-CRM aggregate (new data/automationKpis.js — no real API access to tenant-CRM automations exists, so this is a monthly-totals + recent-failures mock, not a run log). Total Runs/Success Rate/Failure Rate are clickable and drill into Internal Ops Logs with Status + Period presets (Period is a new filter added to that tab for this); Needs Attention (tenants, deduped, with a failure in the last 48h) routes to the Clients list instead, since it's a per-tenant signal, not a run-level one.
2026-07-24 · Added a "CRM Automations" tab to the Clients detail drawer (Tenant 360) — per-tenant, last-7d view of the tenant's own CRM automation runs (lead routing, WhatsApp, field updates), distinct from the existing per-tenant "Automations" tab (Internal Ops). New data/tenantCrmAutomations.js seeds automation configs + run logs for 8 representative tenants, consistent with the failure timing already mocked in data/automationKpis.js. Counts (Configured / Runs 7d / Failed 7d) are clickable and filter+scroll the run table; a "Needs attention" banner appears only when a run failed in the last 48h. Automation names show a tooltip explaining that deep-linking into the tenant's own LEDSAK instance isn't available yet, rather than faking a link; row expansion is a plain inline toggle rather than reusing AutomationRunDrawer, which is hard-coupled to Internal Ops' run shape — both gaps logged as open questions per the originating prompt's instruction, not built now.
2026-07-24 · CustomerSuccessPage fully rebuilt from single At-Risk view into a 4-tab module: At-Risk Accounts (now with Playbook column showing task progress, conflict-aware AssignPlaybookModal, LogContactModal replacing simple "Reach out"), Playbook Library (full CRUD — create/edit/duplicate/toggle, PlaybookForm with reorderable steps, SLA per step), Tenant Tasks (cross-tenant task view — live KPIs, status/AM/search filters, TaskActionsMenu shared with Tenant360, overdue row highlighting, Mark Done/Skip/Note modals), Renewals (due-in-60d pipeline with overdue section, per-tenant renewal status dropdown in local state). Source path updated to superadmin/src/pages/CustomerSuccessPage.jsx.
2026-07-24 · Updated source file paths across all 19 feature docs from the old monolithic LedsakSuperAdmin.jsx to the correct superadmin/src/pages/*.jsx files (project was refactored from single file to per-page modules).
2026-07-24 · HealthPage fully rebuilt from a 26-line stub (4 hardcoded KPIs + 4-row table) into a 5-tab "System Health" ops dashboard: Status Board (20 seeded services across 8 categories, live-computed Overall Status banner, uptime/latency/staleness thresholds, detail drawer with 30-day uptime history + dependency graph + related incidents), Performance (API/DB/cache metrics, slowest endpoints, Queue Monitor link), Incidents (active-incident banners, full create → assign → note → escalate → resolve workflow with a vertical timeline), Deployments (history + Post-Deploy Health before/after comparison + rollback), Dependencies (10 external providers with latency-vs-baseline and real status-page links). New data/health.js seed module; StoreContext.jsx gains 5 thin audit methods (createIncident, resolveIncident, escalateIncident, changeIncidentOwner, rollbackDeploy) — all other state is local to HealthPage per QueuesPage's existing precedent, not promoted to the global store. "health" added to FIXED_HEIGHT_PAGES so the Status Board table scrolls internally like Clients/Logs/Security.
